# TravelDot

> **Every Dot Tells a Story.**

TravelDot 是一款專為旅行者設計的「地圖式旅行記錄 App」。不同於導航軟體或社群媒體，它的核心價值在於成為您的「回憶容器」，讓旅行記憶與地理位置產生永久連結，並支援隨時優雅地回顧。

## 📖 專案背景與定位

在現有的旅行記錄工具中，我們發現了以下未被滿足的需求：
- **Google Maps**: 功能強大但偏向「功能性導航」，缺乏「情感回憶」的承載。
- **相簿 App**: 以時間軸為主，難以還原「空間移動」的旅行軌跡。
- **Instagram**: 偏向精選後的社群展示，而非私人、完整的真實紀錄。

TravelDot 致力於解決這些問題，提供一個專注於**地圖視覺化**與**深度內容**的私人紀錄空間。

## 🚀 核心產品需求

### 1. 極速記錄 (Capture)
- **目標**: 從開啟 App 到完成一個地點的記錄不超過 **30 秒**。
- **情境**: 旅行當下，快速定位、拍照、留下一句短評。

### 2. 智慧整理 (Organize)
- **目標**: 整理一趟 10 天旅程（約 30 個地點）不超過 **1 小時**。
- **情境**: 旅行後，透過自動讀取照片 EXIF（GPS & 時間），自動建議地點分組，大幅降低整理門檻。

### 3. 直覺回顧 (Review)
- **目標**: 找到特定回憶不超過 **10 秒**。
- **情境**: 透過縮放地圖與地點聚合（Clustering），快速跳轉至特定城市或景點回顧細節。

## 🛠️ 功能規格 (MVP)

本專案初期將專注於以下 MVP (Minimum Viable Product) 功能：

| 模組 | 主要功能 |
| :--- | :--- |
| **地圖介面** | Google Maps 整合、地點標記 (Pin)、地點聚合 (Clustering)、響應式設計 |
| **新增地點** | Google Places 搜尋、GPS 定位、手動選點 |
| **內容編輯** | WYSIWYG 文字編輯器、照片批次上傳、日期/標籤/評分管理 |
| **旅程管理** | 旅程建立/編輯、封面設定、日期區間管理 |
| **瀏覽模式** | 地圖模式 (視覺化)、列表模式 (時間軸) |
| **資料匯出** | 完整資料打包 (ZIP)、JSON 格式匯出 |
| **分享功能** | 單一地點/完整旅程網頁分享 (唯讀連結) |

## 🏗️ 技術架構

### 前端 (Frontend)
- **Framework**: React + TypeScript
- **Build Tool**: Vite
- **Styling**: Tailwind CSS + shadcn/ui
- **State Management**: Zustand / Jotai
- **Map Provider**: Google Maps JavaScript API
- **Editor**: TipTap (ProseMirror based)

### 後端 / 服務 (Backend / Services)
- **Platform**: Firebase
- **Auth**: Firebase Authentication (Email/Password)
- **Database**: Cloud Firestore (NoSQL)
- **Storage**: Cloud Storage (Media files)

## 📂 資料結構概念

系統採用階層式的資料架構設計：

```mermaid
graph LR
    Trip[旅程 (Trip)] --> Dest[目的地 (Destination)]
    Dest --> Place[地點 (Place)]
    Place --> Memory[記錄內容 (Memory)]
```

*例如：2024 泰國之旅 (Trip) -> 清邁 (Destination) -> Cafe Amazon (Place) -> 照片+心得 (Memory)*

## 🚦 快速開始 (Getting Started)

1. **環境準備**
   確保已安裝 Node.js (v18+) 與 npm。

2. **安裝依賴**
   ```bash
   npm install
   ```

3. **設定環境變數**
   複製 `.env.example` 為 `.env` 並填入您的 API Keys：
   ```env
   VITE_GOOGLE_MAPS_API_KEY=your_key_here
   VITE_FIREBASE_CONFIG=...
   ```

4. **啟動開發伺服器**
   ```bash
   npm run dev
   ```

## 📝 相關文件
- **[PLAN (專案規劃)](./docs/PLAN.md)**: 商業需求與市場分析，包含專案目標、獲利模式、目標用戶與競爭分析。
- **[PRD (產品需求)](./docs/PRD.md)**: 詳細功能規格、資料結構與開發計畫。
- **[.agent/rules/project-rules.md](./.agent/rules/project-rules.md)**: 專案開發規範與 UI/UX 設計準則。
## 📊 專案狀態 (Project Status)

| Phase | 功能模組 | 狀態 | 進度說明 |
| :--- | :--- | :--- | :--- |
| **Phase 1** | **核心地圖功能** | ✅ 完成 | Google Maps 整合、地點聚合、POI 互動 |
| **Phase 2** | **內容編輯器** | ✅ 完成 | 地點新增/編輯、照片上傳、標籤管理 |
| **Phase 3** | **後端整合** | ✅ 完成 | Firebase Auth、Firestore CRUD、Storage |
| **Phase 4** | **旅程管理** | 🚧 待開發 | 列表模式、篩選功能、Landing Page |
| **Phase 5** | **匯出與分享** | ⏳ 規劃中 | 資料匯出 (ZIP)、公開分享頁面 |

## 🔄 開發流程 (Development Workflow)

本專案採用以下分支策略以確保程式碼穩定性：

- **`main`**: **穩定分支 (Production)**。隨時可部署的版本，僅接受來自 `dev` 的合併。
- **`dev`**: **開發分支 (Development)**。日常開發的主要分支，所有新功能在此整合。
- **`feat/*`**: **功能分支**。開發特定功能時從 `dev` 切出，完成後合併回 `dev`。

### 自動化指令
- **`/git-sync`**: 自動執行 `git status` -> `git add` -> `git commit` -> `git push` 流程，確保程式碼同步。

---
*Developed by Max*
